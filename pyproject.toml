[project]
name = "traffic-project"
version = "0.1.0"
description = "Traffic signal control"
requires-python = ">=3.10"

dependencies = [
    "numpy",
    "pandas",
    "openai",
    "fastapi",
    "uvicorn",
    "openenv-core>=0.2.0"
]

[project.scripts]
server = "server.app:main"
